import Controller.DFS;
import Controller.GBFS;
import Domain.Table;
import UI.UI;

public class Main {
    public static void main(String[] args) {
        UI ui = new UI();
        ui.showMainMenu();
    }
}
